apiVersion: v1
kind: Service
metadata:
  name: nocodb-service
spec:
  type: NodePort
  selector:
    app: nocodb
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 3050
